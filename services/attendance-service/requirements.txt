fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# JWT verification (public key from Auth Service JWKS)
PyJWT[crypto]==2.8.0

# Cryptography (ECDSA signature verification)
cryptography==42.0.7

# AWS (SQS + EventBridge)
boto3==1.34.69
botocore==1.34.69
aioboto3==12.3.0    # Async boto3 for FastAPI endpoints (producer)

# HTTP client (calls Auth Service /pubkey)
httpx==0.27.0

# Validation + settings
pydantic==2.7.1
pydantic-settings==2.2.1

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
